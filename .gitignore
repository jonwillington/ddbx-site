# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Worker-side state left over from the pre-split monorepo. The worker now
# lives in the ddbx-data repo; if you need local worker secrets, move
# .dev.vars there. Safe to delete locally.
.dev.vars
.wrangler

# Editor directories and files
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?


pnpm-lock.yaml
yarn.lock
bun.lockb
# Master cuts of the brand films. The web-weight encodes live in public/film/
# (see src/components/download/story-film.tsx); the masters are too heavy to ship.
film-src/
