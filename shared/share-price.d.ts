// Types for shared/share-price.js. See shared/seo.d.ts for why the module is
// plain ESM with its types declared alongside.

export function sharePrice(
  major: number | null | undefined,
  currency: string | null | undefined,
): string;
