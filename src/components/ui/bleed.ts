/** Full-bleed helpers, re-exported so the ui/ primitives are one import
 *  path. They live in components/full-bleed.ts, where the reasoning is. */
export {
  FULL_BLEED,
  FULL_BLEED_PADDED,
  SCROLL_BLEED,
} from "@/components/full-bleed";
